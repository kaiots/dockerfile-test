name: Docker Image CI

on: [push]
#  push:
#    branches:
#      - 'master'
jobs:
  build:
    runs-on: python:3.11-alpine3.18
    steps:
    - uses: actions/checkout@v2
    - name: docker build 
      run: |
        docker build ./
