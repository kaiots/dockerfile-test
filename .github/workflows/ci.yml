name: Docker Image CI

on: [push]
#  push:
#    branches:
#      - 'master'
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: docker build csharp
      run: |
        docker build ./
